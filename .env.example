# SMTP Configuration
# Use your authenticated domain email as SMTP_USER
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@yourdomain.com
SMTP_PASS=your-app-password

# Email recipient (your admin email where contact form submissions will be sent)
CONTACT_RECEIVER=admin@yourdomain.com
